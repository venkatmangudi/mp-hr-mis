<%- model_class = SpecialCadre -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human %></h1>
</div>

<dl class="dl-horizontal">
  <dt><strong><%= model_class.human_attribute_name(:Special_Cadre_Type) %>:</strong></dt>
  <dd><%= @special_cadre.Special_Cadre_Type %></dd>
</dl>

<div class="form-actions">
  <%= link_to t('.back', :default => t("helpers.links.back")),
              special_cadres_path, :class => 'btn'  %>
  <%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_special_cadre_path(@special_cadre), :class => 'btn' %>
  <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              special_cadre_path(@special_cadre),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
</div>
