<%= simple_form_for @gender, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :gender_name %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                genders_path, :class => 'btn' %>
  </div>
<% end %>
