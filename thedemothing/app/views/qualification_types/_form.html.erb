<%= simple_form_for @qualification_type, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :Qualification_type %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                qualification_types_path, :class => 'btn' %>
  </div>
<% end %>
