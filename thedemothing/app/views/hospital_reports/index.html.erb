<%- model_class = HospitalReport -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize %></h1>
</div>
<table class="table table-bordered" id="employe">
  <thead>
    <tr>
      <th>Hospital Full Name</th>
      
    </tr>
  </thead>
  <tbody>
    <% @hospital_reports.each do |hospital_report| %>
      <tr>
        <td><%= link_to hospital_report.NewReports, hospital_report_path(hospital_report) %></td>
        
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_hospital_path,
            :class => 'btn' %>
            
<script>
    $(document).ready(function() {
      $('#employe').dataTable( {
        "sPaginationType": "bootstrap"
      } );
    }
  );
</script>
