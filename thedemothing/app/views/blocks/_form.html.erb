<%= simple_form_for @block, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :balock_name %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                blocks_path, :class => 'btn' %>
  </div>
<% end %>
