<%= simple_form_for @pincode, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :pincode %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                pincodes_path, :class => 'btn' %>
  </div>
<% end %>
