<%= simple_form_for @taluk, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :taluk_name %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                taluks_path, :class => 'btn' %>
  </div>
<% end %>
